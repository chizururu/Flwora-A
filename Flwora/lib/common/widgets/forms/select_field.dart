import 'package:flutter/material.dart';
import 'package:get/get.dart';

class TSelect<T> extends StatelessWidget {
  const TSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return DropdownMenu(dropdownMenuEntries: []);
  }
}
