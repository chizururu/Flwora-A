import 'package:flutter/material.dart';

class DashboardDeviceScreen extends StatelessWidget {
  const DashboardDeviceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
